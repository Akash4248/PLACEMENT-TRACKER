# CampusTrack Frontend

React + Vite frontend for the Campus Interview Tracking & Result Management System.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` if the backend API is not running at `http://localhost:5000/api`.

## Build

```bash
npm run build
```
